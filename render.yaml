services:
  - type: web
    name: karnataka-village-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
    healthCheckPath: /api/health
    autoDeploy: true
    plan: free
